import React, { useState, useEffect } from 'react';

const Debounce = () => {
  
    
      return (
        <div>
         <h1>Debounce</h1>
        </div>
      );
  
};

export default Debounce;
